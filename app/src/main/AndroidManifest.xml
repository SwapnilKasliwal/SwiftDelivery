<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.swiftdelivery">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwiftDelivery"
        tools:replace="android:theme">
        <activity android:name=".WholesalerAddNewProductActivity"></activity>
        <activity android:name=".WholesalerUserProductActivity" />
        <activity android:name=".WholesalerNewOrdersActivity" />
        <activity android:name=".WholesalerAddProductActivity" />
        <activity android:name=".WholesalerHomeActivity" />
        <activity android:name=".RetailerUserProductsActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.SwiftDelivery.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".logInActivity" />
        <activity android:name=".RetailerHomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProductsCat" />
        <activity android:name=".RetailerAddNewProductActivity" />
        <activity android:name=".RetailerAddProductActivity" />
        <activity android:name=".RetailerNewOrdersActivity" />
        <activity android:name=".ConfirmFinalOrder" />
        <activity android:name=".RetailerOrderProducts"/>
        <activity android:name=".RetailerProductsCat"/>
        <activity android:name=".RetailerProductsDetailsActivity"/>
        <activity android:name=".RetailerYourOrders"/>
        <activity android:name=".RetailerCartActivity"/>
        <activity android:name=".RetailerConfirmFinalOrder"/>
        <activity android:name=".YourOrders"/>
        <activity android:name=".FeedbackActivity"/>
    </application>

</manifest>